import { StyleSheet } from "react-native";
export const defaultStyles = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: 160,
    marginBottom: 110,
    marginHorizontal: 20,
    gap: 20,
  },
});
